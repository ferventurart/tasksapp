import { createAction } from '@ngrx/store';

export const changeCategory = createAction('[Filter Component] Change Category');
export const changeStatus = createAction('[Filter Component] Change Status');